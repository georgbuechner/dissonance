#ifndef SRC_CODES_H_
#define SRC_CODES_H_

enum ViewRange {
  HIDE = 0,
  GRAPH = 1,
  RANGE = 2,
};

enum Units {
  BRONZE_GATHERER = 0,
  SILVER_GATHERER = 1,
  GOLD_GATHERER = 2,
  ATK = 3,
  BARACKS = 4,
  DEFENCE = 5,
};

#endif
